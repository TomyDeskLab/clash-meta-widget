# 安全说明

当前版本：v1.0.0-preview.3，构建 11。范围限于本项目的组件、宿主、权限、构建产物与控制路径，不覆盖 ClashX Meta、其特权助手、代理服务商或 macOS。此记录不是独立安全认证，也不能证明不存在漏洞。

## 报告安全问题

请通过仓库的 **Security → Report a vulnerability** 私下报告可能导致未授权代理切换、凭据泄露或沙盒越界的问题。不要在公开 Issue 中附上订阅、控制接口密钥、节点服务器参数、状态快照或完整操作 URL。仅维护当前发布版本与主分支。

## 已采取措施

- 手动延迟测试通过固定本机控制接口调用 `/proxies/{编码名称}/delay`，让 Meta 经该节点访问固定的 `https://www.gstatic.com/generate_204`。5 秒节点超时，最多并发 3 项；不使用会清除自动策略组固定选择的组批量测速接口，不发送选择或模式变更。它产生少量外网探测流量；本工具不把接口密钥传给测试站点。接口依据 [Meta 官方 API 文档](https://wiki.metacubex.one/api/)。

- 不读订阅地址、节点服务器参数、节点凭据或浏览历史；宿主只连接固定主机 `127.0.0.1` 上用户配置的 Meta 控制端口，默认 `9090`。没有监听端口、遥测、自动下载或更新执行代码。
- 可选控制接口密钥保存在 macOS 钥匙串，属性为“仅本设备、首次解锁后可用”；不写入 UserDefaults、快照或日志。系统代理端口和控制端口不是秘密，保存在应用偏好设置。
- 组件保持 App Sandbox，仅允许只读 `~/Library/Application Support/ClashMetaSwitch/`。宿主无管理员权限，不安装特权助手，仅使用系统自动化授权调用已有 ClashX Meta。
- 固定 AppleScript 源码，目标固定为 `com.metacubex.ClashX.meta`；不将 URL 或用户输入拼接进脚本。
- 点击 URL 只能是固定 scheme 和 host，拒绝 query、fragment、user info、port 及错误路径。授权动作来自本地快照，不从 URL 接受命令、脚本或目标地址。
- 模式仅允许 `rule/global/direct`。节点箭头从操作时重新读取的当前 Selector 列表选择，旧的指定名称入口还必须通过私有快照白名单；快照使用随机操作凭据，不是数字签名。策略组路径编码保留字、分隔符和非 ASCII 字符，请求禁止重定向且不会使用系统代理。
- 每个凭据含 256 位系统安全随机数；在比较时检查长度并逐字节比较。模式凭据按动作区分，开关凭据不能用来切换节点或模式。
- 使用可重复的本地凭据，不因刷新、状态变化或使用而轮换，也不设短期到期时间，以兼容 WidgetKit 缓存卡片。这是明确的安全取舍：同一链接可以再次执行，不提供防重放保证。宿主拒绝并发操作，并用 0.8 秒间隔过滤快速重复唤起；这不是防重放机制。
- 快照目录权限 0700、文件 0600；先创建私有临时文件再原子替换。扩展没有写入权限。
- 检测到其他代理、PAC/WPAD、Meta 没运行、系统状态读不到时拒绝操作。拒绝授权和状态不符合预期时显示错误，不宣称成功。
- 发布构建使用 Hardened Runtime，未启用 get-task-allow、未关闭 library validation；`codesign --verify --deep --strict` 验证完整性。临时本地签名不是 Apple Developer ID 或公证。

## 已验证

- Release 编译通过；安装产物完整性校验通过。
- 当前构建的 82 项独立输入及状态检查通过，包括伪造/修改请求、凭据撤销、缓存链接重复使用、跨动作凭据、非法节点、异常路径、自动端口、控制接口路径边界和其他代理/PAC/WPAD。
- 运行中快照文件权限为 0600，目录为 0700。
- 当前节点窗口的单项、8 项批量测速和停止均经实际点击验证；测速前后全部 Selector 选择、模式和系统代理状态一致。
- 宿主与扩展均包含 arm64、x86_64；运行日志确认扩展可读快照，下载 ZIP 与已验证安装产物一致。
- 控制链路与未覆盖范围统一记录在 [当前版本测试记录](./docs/TEST_REPORT.md)。
- 桌面实际点击结果仍需确认；不将宿主按钮成功冒充桌面端到端成功。

## 限制

- 自定义 URL 是受随机凭据保护的本地入口。若同一登录用户下的恶意非沙盒程序读取了快照或系统保存的启动 URL，就可能重复执行其允许的操作；这不抵御已经控制本机用户会话的攻击者。不应复制或分享快照中的 ticket 或完整动作 URL。怀疑泄露时，先退出宿主，把私有快照移出原目录，再启动生成新凭据并重新添加卡片；旧链接即失效。
- 为了由无网络权限的小组件显示选择项，快照会保存当前策略组的节点显示名称，但不保存节点地址、端口或认证信息。
- 测试机的 Meta 控制接口无需认证即可访问；这不是新增监听端口，组件也没有改变该设置。其他本机进程仍可能直接访问该接口，URL 操作凭据并不能保护 Meta 自身的接口。共享版支持 Bearer 密钥，但不能替代 Meta 端正确配置监听地址和认证。
- App Sandbox 的只读例外只限制组件；宿主是普通非沙盒应用，安全范围依赖可审查源码、系统进程隔离及自动化授权。没有宣称“绝对安全”。
- 构建 11 从正在运行的 Meta 进程定位其生成配置，只接受固定应用数据目录中的单个 YAML 文件，再从本机控制接口读取实际 HTTP、SOCKS 和混合端口。进程参数仅用于发现允许目录内的配置路径，不写入或公开配置内容。接口与端口无法可靠确认时拒绝开关；检测到其他地址、其他端口或 PAC/WPAD 时也拒绝操作。系统代理不等同于 TUN/VPN 或网络可达性。
- 系统刷新预算可能使组件显示旧状态；开关按执行时的真实状态反转，节点箭头按当前模式对应策略组的实时选择前后移动，模式按点击的明确值设置。结果仍要等待 macOS 刷新显示。
