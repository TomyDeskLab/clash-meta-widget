# 本次安全检查记录

范围：本目录原生组件及本地宿主源码、权限清单、构建产物和开关路径。不覆盖 ClashX Meta、其特权助手、代理服务商、macOS 或整个 Hako 项目。此记录不是独立安全认证，也不能证明不存在任何漏洞。

## 报告安全问题

公开到 GitHub 后，请通过仓库的 **Security → Report a vulnerability** 私下报告可能导致未授权代理切换、凭据泄露或沙盒越界的问题。不要在公开 Issue 中附上订阅、控制接口密钥、节点服务器参数、状态快照或完整操作 URL。当前维护版本为最新主分支；尚未承诺旧版本的安全更新期限。

## 已采取措施

- 不读订阅地址、节点服务器参数、节点凭据或浏览历史；宿主只连接固定主机 `127.0.0.1` 上用户配置的 Meta 控制端口，默认 `9090`。没有监听端口、遥测、自动下载或更新执行代码。
- 可选控制接口密钥保存在 macOS 钥匙串，属性为“仅本设备、首次解锁后可用”；不写入 UserDefaults、快照或日志。系统代理端口和控制端口不是秘密，保存在应用偏好设置。
- 组件保持 App Sandbox，仅允许只读 `~/Library/Application Support/ClashMetaSwitch/`。宿主无管理员权限，不安装特权助手，仅使用系统自动化授权调用已有 ClashX Meta。
- 固定 AppleScript 源码，目标固定为 `com.metacubex.ClashX.meta`；不将 URL 或用户输入拼接进脚本。
- 点击 URL 只能是固定 scheme 和 host，拒绝 query、fragment、user info、port 及错误路径。授权动作来自本地快照，不从 URL 接受命令、脚本或目标地址。
- 模式仅允许 `rule/global/direct`。节点必须同时出现在本地私有快照和操作时重新读取的当前 Selector 列表中；快照使用随机操作凭据，不是数字签名。策略组路径编码保留字、分隔符和非 ASCII 字符，请求禁止重定向且不会使用系统代理。
- 每个凭据含 256 位系统安全随机数；在比较时检查长度并逐字节比较。先写入替代凭据使旧值失效，再执行系统变更。存储失败时不执行。
- 凭据最长 24 小时有效，状态变化或使用后轮换。防止伪造、重放及双击重复切换。过期点击只显示说明并请求刷新。
- 快照目录权限 0700、文件 0600；先创建私有临时文件再原子替换。扩展没有写入权限。
- 检测到其他代理、PAC/WPAD、Meta 没运行、系统状态读不到时拒绝操作。拒绝授权和状态不符合预期时显示错误，不宣称成功。
- 发布构建使用 Hardened Runtime，未启用 get-task-allow、未关闭 library validation；`codesign --verify --deep --strict` 验证完整性。临时本地签名不是 Apple Developer ID 或公证。

## 已验证

- Release 编译通过；安装产物完整性校验通过。
- 46 项独立输入及状态检查通过，包括伪造/修改/过期/重放请求、跨动作凭据、非法节点、异常路径、端口边界和其他代理/PAC/WPAD。
- 运行中快照文件权限为 0600，目录为 0700。
- 应用内实际点击“关闭系统代理”后读到关闭；随后状态恢复开启，并核对 HTTP、HTTPS、SOCKS 均为 127.0.0.1:7890 且启用。
- 构建 6 通过与组件一致的 URL 入口实测节点变更、恢复，以及规则、全局、直连三种模式；均重新读取 Meta 确认。构建 7 沿用这条控制链路并新增端口和钥匙串设置。
- 构建 7 通过 arm64 与 x86_64 通用编译和签名结构检查。运行中的 WidgetKit 扩展日志确认可读快照；操作系统连接表确认测试机的现有控制接口由 Meta 进程监听在 `127.0.0.1:9090`。
- 桌面实际点击结果仍需确认；不将宿主按钮成功冒充桌面端到端成功。

## 限制

- 自定义 URL 是受随机凭据保护的本地入口。若同一登录用户下的恶意非沙盒程序读取了快照或系统保存的启动 URL，则可能抢先使用尚未消费的凭据；这不抵御已经控制本机用户会话的攻击者。不应复制或分享快照中的 ticket 或完整动作 URL。
- 为了由无网络权限的小组件显示选择项，快照会保存当前策略组的节点显示名称，但不保存节点地址、端口或认证信息。
- 测试机的 Meta 控制接口无需认证即可访问；这不是新增监听端口，组件也没有改变该设置。其他本机进程仍可能直接访问该接口，URL 操作凭据并不能保护 Meta 自身的接口。共享版支持 Bearer 密钥，但不能替代 Meta 端正确配置监听地址和认证。
- App Sandbox 的只读例外只限制组件；宿主是普通非沙盒应用，安全范围依赖可审查源码、系统进程隔离及自动化授权。没有宣称“绝对安全”。
- 组件根据用户设置的本地端口识别当前代理，默认 `7890`。若其他软件也占用相同端口，可能被误认为 Meta；检测到其他地址、其他端口或 PAC/WPAD 时会拒绝操作。系统代理不等同于 TUN/VPN 或网络可达性。
- 系统刷新预算可能使组件短暂显示旧状态；执行时会重新检查真实状态，并按明确的目标状态操作。
